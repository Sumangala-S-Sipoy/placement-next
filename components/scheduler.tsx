"use client"

import React from "react"

export function Scheduler(): React.ReactElement | null {
  // Events removed from the system — keep component present to avoid import errors.
  return null
}

export default Scheduler
